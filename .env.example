# Supabase Configuration
# Get these from: https://app.supabase.com/project/_/settings/api

# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Your Supabase anon/public key (safe to expose)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Your Supabase service role key (NEVER expose to client, server-side only)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Optional: Secret for cron job authentication (for auto-clear endpoint)
# Generate a random string: openssl rand -hex 32
CRON_SECRET=sA3Lng0Jlpy0KaIOZ0W+39JAdNfbAeI1B5/aBnSyt/o=

# =============================================================================
# SETUP INSTRUCTIONS:
# =============================================================================
# 1. Copy this file to .env.local
# 2. Fill in your actual Supabase credentials
# 3. Never commit .env.local to git (it's in .gitignore)
# 4. Set CRON_SECRET if using external cron services like Vercel Cron
# =============================================================================
